extends layout

block content
    #header.page-header
        h1 #{title}
            small 很简单！
    #message.hide
    #searching.row
        .span6.offset3
            form#search-form.well.form-search.center(action="#", method="post")
                input#input.input-medium.search-query(name="word", required="true", type="text", placeholder="请输入查询请求")
                input#submit.btn.btn-primary(type="submit", value="提交")
    #results.row.hide
        ul#result-list.pager
        #download.row
            .span6.offset3.center
                button#downloadAll.btn.btn-success.btn-large 一键下载所有
    #footer.row.center Copyright &copy; 2012 LTW
